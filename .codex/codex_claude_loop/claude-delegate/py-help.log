usage: delegate_to_claude.py [-h] --task-file TASK_FILE
                             [--task-mode {implementation,rework}]
                             [--session-mode {PrimaryReuse,PrimaryAnchor,ParallelPool}]
                             [--session-key SESSION_KEY]
                             [--allowed-path ALLOWED_PATH]
                             [--validation-command VALIDATION_COMMAND]
                             [--artifact-root ARTIFACT_ROOT] [--model MODEL]
                             [--name-prefix NAME_PREFIX] [--round ROUND]
                             [--max-round MAX_ROUND]
                             [--max-parallel MAX_PARALLEL]
                             [--lease-ttl-seconds LEASE_TTL_SECONDS]
                             [--lease-wait-seconds LEASE_WAIT_SECONDS]
                             [--max-retry-count MAX_RETRY_COUNT]
                             [--bypass-permissions] [--dry-run] [--start-only]
                             [--prepare-only] [--worker-config WORKER_CONFIG]

Codex Claude Loop delegate runtime

options:
  -h, --help            show this help message and exit
  --task-file TASK_FILE
  --task-mode {implementation,rework}
  --session-mode {PrimaryReuse,PrimaryAnchor,ParallelPool}
  --session-key SESSION_KEY
  --allowed-path ALLOWED_PATH
  --validation-command VALIDATION_COMMAND
  --artifact-root ARTIFACT_ROOT
  --model MODEL
  --name-prefix NAME_PREFIX
  --round ROUND
  --max-round MAX_ROUND
  --max-parallel MAX_PARALLEL
  --lease-ttl-seconds LEASE_TTL_SECONDS
  --lease-wait-seconds LEASE_WAIT_SECONDS
  --max-retry-count MAX_RETRY_COUNT
  --bypass-permissions
  --dry-run
  --start-only
  --prepare-only
  --worker-config WORKER_CONFIG
